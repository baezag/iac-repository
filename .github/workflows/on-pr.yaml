name: Pull Request Workflow

on:
  workflow_dispatch:
  pull_request:

jobs:
  terraform-checks:
    uses: baezag/iac-components/.github/workflows/test.yaml@dev
    with:
      environment: ${{ github.ref_name }}
      workspace: ${{ vars.workspace }}
    secrets: inherit
    